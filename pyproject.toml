[project]
name = "multi-object-tracking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "pytest>=9.1.1",
    "torch>=2.12.1",
    "torchvision>=0.27.1",
]
